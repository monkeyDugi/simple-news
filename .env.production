# next build (NODE_ENV=production) 시 자동 로드 — Vercel 배포 + Android 정적 빌드 모두 적용
# 시크릿(SUPABASE_SECRET_KEY, OPENAI_API_KEY, CRON_SECRET 등)은 절대 여기 넣지 말 것.
# 시크릿은 .env.local (로컬) 또는 Vercel 대시보드 환경변수에서 관리.
NEXT_PUBLIC_API_BASE_URL=https://news.flowpick.org
SUMMARIZE_BATCH_SIZE=20
